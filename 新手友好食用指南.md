# ShopAgent-X 新手友好食用指南

> 配合 [README.md](./README.md) 和 [ShopAgent-X学习指南.md](./ShopAgent-X学习指南.md) 使用
> README 告诉你"这个项目是什么"，进阶指南告诉你"怎么深入理解"，本指南告诉你"从零开始怎么上手"

---

## 目录

1. [本指南的定位](#1-本指南的定位)
2. [项目背景知识扫盲](#2-项目背景知识扫盲)
3. [环境准备与项目启动](#3-环境准备与项目启动)
4. [项目结构全景图](#4-项目结构全景图)
5. [新手友好学习路线](#5-新手友好学习路线)
6. [核心概念详解](#6-核心概念详解)
7. [代码阅读指引](#7-代码阅读指引)
8. [动手实践建议](#8-动手实践建议)
9. [常见问题与解决方案](#9-常见问题与解决方案)
10. [学习资源推荐](#10-学习资源推荐)

---

## 1. 本指南的定位

**你是什么样的读者？**
- 学完了 Java/Python/Kotlin 基础语法
- 了解 Spring Boot 基本用法（Controller、Service）
- 知道数据库是什么，会写基本 SQL
- 但没有完整的项目开发经验
- 对 AI/Agent/RAG 这些概念只是听说过，不太理解

**本指南和进阶指南的区别：**

| 方面 | 进阶指南 | 本指南（新手版） |
|------|----------|------------------|
| 前置知识 | 假设你已掌握设计模式、AI 基础 | 从零解释每个概念 |
| 学习节奏 | 1-2 天跑通，3-5 天理解数据流 | 每个阶段都给你更多时间 |
| 代码阅读 | 直接看核心代码 | 先看入口，再看调用链 |
| 实践建议 | 写测试、改代码 | 先跑通、再理解、最后动手 |
| 目标 | 面试能讲清楚 | 能看懂代码、理解架构 |

---

## 2. 项目背景知识扫盲

### 2.1 什么是 Agent？

**简单理解**：Agent 就是一个能"自主思考、自主行动"的 AI 程序。

传统程序：
```
用户输入 → 程序处理 → 输出结果
（程序的每一步都是程序员写死的）
```

Agent 程序：
```
用户输入 → AI 思考 → 决定做什么 → 调用工具 → 继续思考 → ... → 输出结果
（AI 可以根据情况动态决定下一步做什么）
```

**举个例子**：
- 传统程序：用户问"推荐跑鞋"，程序直接从数据库查商品返回
- Agent 程序：用户问"推荐跑鞋"，AI 思考后决定：
  1. 先理解用户意图（是商品推荐还是闲聊？）
  2. 从知识库中检索相关商品
  3. 用四级搜索流水线精确匹配
  4. 生成个性化推荐话术
  5. 防幻觉校验确保商品信息准确

**本项目的 Agent**：就是一个智能电商导购助手，能理解用户需求，自动决定怎么处理，最终给出准确的商品推荐。

---

### 2.2 什么是 RAG？

**RAG = Retrieval-Augmented Generation（检索增强生成）**

**为什么需要 RAG？**
- 大语言模型（LLM）的知识是训练时学到的，有截止日期
- LLM 不知道你店铺里有什么商品、价格是多少
- 直接问 LLM，它可能"编造"商品信息（幻觉）

**RAG 怎么解决？**
```
用户问题
    ↓
① 检索：从你的商品库中找到相关商品
    ↓
② 增强：把找到的商品信息作为"参考资料"
    ↓
③ 生成：让 LLM 基于参考资料生成推荐话术
```

**本项目的 RAG 流程**：
1. 用户问"推荐跑鞋"
2. 系统从商品库中搜索相关商品（向量相似度 + SQL 精确匹配）
3. 找到 5 款相关跑鞋
4. 把这 5 款商品信息 + 用户问题一起发给 LLM
5. LLM 基于这些商品生成个性化推荐话术，并附带商品卡片

---

### 2.3 什么是向量数据库？

**传统数据库**：存的是文字、数字，搜索靠精确匹配
```sql
SELECT * FROM products WHERE name LIKE '%跑鞋%'
```

**向量数据库**：存的是"向量"（一串数字），搜索靠语义相似度
```
用户问题："推荐跑鞋"
    ↓ 转换成向量
[0.12, 0.85, 0.33, ...]
    ↓ 和商品库中的向量比较相似度
找到最相似的 5 款商品
```

**为什么要用向量？**
- "跑鞋" 和 "跑步鞋" 文字不同，但语义相似
- 传统搜索找不到，向量搜索能找到

**本项目用的向量数据库**：
- FAISS（本地文件，零依赖）
- 用于商品知识库的语义检索

---

### 2.4 什么是 LLM？

**LLM = Large Language Model（大语言模型）**

就是 ChatGPT、豆包这类 AI 模型。本项目中，LLM 负责：
- 理解用户意图（是商品推荐还是闲聊？）
- 提取搜索关键词和排除词
- 判断检索结果够不够
- 生成推荐话术
- 防幻觉校验

**本项目使用的 LLM**：
- 豆包 Seed 2.0 Lite（字节跳动，主力模型）
- DashScope（阿里云，用于 Embedding）

---

### 2.5 什么是 Tool？

**Tool = 工具**，就是 Agent 可以调用的"能力"。

**类比**：Agent 像一个项目经理，Tool 像项目经理手下的员工
- 项目经理（Agent）负责思考、决策、分配任务
- 员工（Tool）负责具体执行，比如搜索商品、读取记忆、管理购物车

**本项目的 Tool**：
```
Tool 名称              功能                    超时时间
───────────────────────────────────────────────────────
knowledge_search       商品知识库检索          30 秒
question_rewrite       问题改写                10 秒
rerank                 结果重排序              10 秒
memory_read            读取对话记忆            5 秒
memory_write           写入对话记忆            5 秒
ocr_extract            OCR 图片文字提取        60 秒
cart_tool              购物车 CRUD             10 秒
```

---

### 2.6 什么是多 Agent 协作？

**简单理解**：多个 Agent 各司其职，协同完成任务。

**类比**：一个公司有不同部门
- 前台（RouterAgent）：接待客户，判断需求，分发给对应部门
- 销售部（ShoppingAgent）：处理商品推荐
- 客服部（KnowledgeQAAgent）：回答知识问题
- 前台接待（ChitChatAgent）：处理闲聊
- 分析部（ReasoningAgent）：处理复杂推理

**本项目的 Agent 协作**：
```
用户: "推荐跑鞋"
    ↓
RouterAgent 识别意图 → shopping
    ↓
分发给 ShoppingAgent
    ↓
ShoppingAgent 执行四级搜索 → 生成推荐 → 返回商品卡片
```

---

## 3. 环境准备与项目启动

### 3.1 环境要求

**必须安装**：
```
Java 17+          → Spring Boot 后端运行
Python 3.10+      → AI 服务运行
Node.js 18+       → 前端开发
MySQL 8.0+        → 数据存储
Redis 7+          → 缓存和会话记忆
Android Studio    → 编译 Android 客户端
```

**可选安装**：
```
Docker            → 一键部署 MySQL + Redis（学习阶段可以手动安装）
```

---

### 3.2 项目启动步骤

**第一步：克隆项目**
```bash
git clone https://github.com/EvanYao826/ShopAgent-X.git
cd ShopAgent-X
```

**第二步：启动数据库**
```bash
# 方式一：Docker 一键启动（推荐）
docker-compose up -d    # MySQL 8.0 (3306) + Redis 7 (6379)

# 方式二：手动启动
# 启动 MySQL，创建数据库，执行 backend/sql/init.sql 初始化表结构
# 启动 Redis
```

**第三步：启动 Python AI 服务**
```bash
cd backend/python-service

# 创建虚拟环境
python -m venv venv
venv\Scripts\activate    # Windows
# source venv/bin/activate  # Linux/Mac

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入 DOUBAO_API_KEY

# 启动服务
python main.py          # 端口 8000
```

**第四步：启动 Java 后端**
```bash
cd backend

# 修改 application.yml 中的数据库连接信息（如果需要）

# 启动 Spring Boot 应用
mvn spring-boot:run     # 端口 8888
```

**第五步：启动 React 前端（管理端）**
```bash
cd backend/frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev             # 端口 5173
```

**第六步：编译 Android 客户端**
```
Android Studio 打开 android/ 目录
Gradle 同步后编译运行
```

**第七步：访问系统**
```
管理端：http://localhost:5173
默认管理员：admin / admin123
Android 客户端：安装到手机或模拟器
```

---

### 3.3 启动常见问题

**问题 1：Python 服务启动失败，提示缺模块**
```bash
# 解决：确保在虚拟环境中安装依赖
pip install -r requirements.txt
```

**问题 2：Java 后端连接数据库失败**
```bash
# 解决：检查 application.yml 中的数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop_agent
    username: root
    password: your_password
```

**问题 3：前端启动失败**
```bash
# 解决：清除缓存重新安装
rm -rf node_modules
npm install
npm run dev
```

**问题 4：Android 编译失败**
```
# 解决：检查 local.properties 中的 SDK 路径
# 确保 Android Studio 已安装对应版本的 SDK
```

---

## 4. 项目结构全景图

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     客户端层                                  │
│  ┌──────────────────┐  ┌──────────────────┐                 │
│  │  Android App     │  │  React 管理端     │                 │
│  │  Kotlin Compose  │  │  Vite + React 18 │                 │
│  └────────┬─────────┘  └────────┬─────────┘                 │
└───────────┼─────────────────────┼───────────────────────────┘
            │ HTTP/SSE            │ HTTP
            ▼                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  Java 后端 (Spring Boot 3.2)                 │
│            (用户认证、商品管理、API 网关)                     │
└───────────────────────┬─────────────────────────────────────┘
                        │ HTTP
                        ▼
┌─────────────────────────────────────────────────────────────┐
│               Python AI 服务 (FastAPI)                       │
│              (Agent 核心、RAG 流程、LLM 调用)                │
└────────────────────────┬────────────────────────────────────┘
                         │
            ┌────────────┼────────────┐
            ▼            ▼            ▼
      ┌──────────┐ ┌──────────┐ ┌──────────┐
      │ MySQL 8  │ │ Redis 7  │ │ FAISS    │
      │ 数据存储  │ │ 缓存+会话 │ │ 向量检索  │
      └──────────┘ └──────────┘ └──────────┘
```

---

### 4.2 目录结构详解

```
ShopAgent-X/
├── android/                     # Android 客户端
│   └── app/src/main/java/.../
│       ├── ui/                  # 页面（15 个 Screen）
│       │   ├── ChatScreen.kt   # 聊天页面 ⭐
│       │   ├── LoginScreen.kt  # 登录页面
│       │   └── ...
│       ├── data/                # 数据层
│       │   ├── api/            # API 接口定义
│       │   ├── repository/     # 数据仓库
│       │   └── model/          # 数据模型
│       └── viewmodel/           # ViewModel（8 个）
│
├── backend/
│   ├── src/main/java/.../       # Java 后端
│   │   ├── controller/          # API 接口（18 个）
│   │   │   ├── ChatController.java    # 聊天接口 ⭐
│   │   │   ├── ProductController.java # 商品接口
│   │   │   └── ...
│   │   ├── service/             # 业务逻辑（25 个）
│   │   │   ├── ChatService.java
│   │   │   ├── AiService.java   # 调用 Python 服务 ⭐
│   │   │   └── ...
│   │   ├── entity/              # 数据实体（25 个）
│   │   └── config/              # 配置
│   │
│   ├── python-service/          # Python AI 服务 ⭐⭐⭐
│   │   ├── agent/               # 五层编排框架
│   │   │   ├── orchestrator.py  # 编排器 ⭐
│   │   │   ├── planner.py       # 规划器 ⭐
│   │   │   ├── executor.py      # 执行器
│   │   │   ├── state.py         # 状态管理
│   │   │   └── events.py        # 事件总线
│   │   ├── workflows/           # 9 个专业 Agent
│   │   │   ├── router_agent.py      # 中央路由 ⭐
│   │   │   ├── shopping_agent.py    # 购物导购 ⭐⭐
│   │   │   ├── knowledge_qa_agent.py # 知识问答
│   │   │   ├── chitchat_agent.py    # 闲聊
│   │   │   └── ...
│   │   ├── tools/               # 7 个 AI 工具
│   │   │   ├── registry.py      # ToolRegistry 单例 ⭐
│   │   │   ├── base.py          # 工具基类
│   │   │   ├── execution.py     # 执行跟踪
│   │   │   └── ...
│   │   ├── core/                # 核心组件
│   │   │   ├── llm.py          # LLM 调用 ⭐
│   │   │   ├── vector_store.py  # FAISS 向量存储
│   │   │   └── config.py        # 配置
│   │   ├── intent/              # 意图分类
│   │   │   └── classifier.py    # IntentClassifier ⭐
│   │   ├── api/                 # FastAPI 路由
│   │   └── main.py              # 应用入口
│   │
│   ├── sql/                     # 数据库脚本
│   │   └── init.sql             # 25 张表 + 示例数据
│   │
│   └── frontend/                # React 管理端
│       └── src/
│           ├── pages/           # 页面组件
│           │   ├── Dashboard.jsx    # 仪表盘
│           │   ├── AgentRuns.jsx    # Agent 执行记录
│           │   └── ...
│           └── components/      # 通用组件
│
├── data/                        # 数据集
│   └── ecommerce_agent_dataset/ # 4 品类 × 25 商品 = 100 条
│
├── docs/                        # 文档
│   ├── 设计文档.md
│   └── 部署与体验指南.md
│
└── docker-compose.yml           # Docker 部署配置
```

---

### 4.3 新手应该关注哪些文件？

**第一优先级（必须看懂）**：
```
backend/python-service/workflows/router_agent.py   → 理解请求怎么分发
backend/python-service/workflows/shopping_agent.py  → 理解商品推荐核心逻辑
backend/python-service/agent/orchestrator.py        → 理解 Agent 编排
backend/python-service/tools/registry.py            → 理解工具怎么管理
```

**第二优先级（建议看懂）**：
```
backend/python-service/agent/planner.py             → 理解 Agent 怎么决策
backend/python-service/agent/executor.py            → 理解 Agent 怎么执行
backend/python-service/intent/classifier.py         → 理解意图分类
backend/python-service/core/llm.py                  → 理解 LLM 调用
```

**第三优先级（了解即可）**：
```
backend/src/main/java/.../controller/ChatController.java → Java 聊天接口
backend/src/main/java/.../service/AiService.java         → 调用 Python 服务
android/.../ui/ChatScreen.kt                             → Android 聊天页面
```

---

## 5. 新手友好学习路线

### 第一周：跑通项目，建立信心

**目标**：把项目跑起来，看到效果

**Day 1-2：环境搭建**
```
✅ 安装 Java、Python、Node.js、MySQL、Redis
✅ 克隆项目，配置数据库
✅ 启动 Python AI 服务、Java 后端、React 前端
✅ 编译 Android 客户端
✅ 访问管理端，看到仪表盘
```

**Day 3-4：体验功能**
```
✅ Android 端：试试商品推荐（问"推荐跑鞋"）
✅ Android 端：试试闲聊（问"你好"）
✅ Android 端：试试拍照识图
✅ 管理端：看看 Agent 执行记录
✅ 管理端：看看仪表盘数据
```

**Day 5-7：初步理解**
```
✅ 阅读 README.md，了解项目背景
✅ 阅读本指南的"背景知识扫盲"部分
✅ 画出项目的三端架构图（Android、Java 后端、Python AI 服务）
```

**验证**：能向别人解释"这个项目是干什么的"

---

### 第二周：理解请求流程

**目标**：理解一个请求从前端到后端到 AI 服务的完整链路

**Day 1-2：Android 到 Java 后端**
```
✅ 打开 android/.../data/api/ 目录
✅ 找到发送消息的 API 定义
✅ 打开 Java 后端的 ChatController.java
✅ 找到处理消息的接口
✅ 理解 Android 怎么调用后端
```

**Day 3-4：Java 后端到 Python AI 服务**
```
✅ 打开 Java 后端的 AiService.java
✅ 找到调用 Python 服务的代码
✅ 打开 Python 服务的 api/routes.py
✅ 找到接收请求的接口
✅ 理解 Java 后端怎么调用 Python 服务
```

**Day 5-7：Python AI 服务内部**
```
✅ 打开 workflows/router_agent.py，找到 handle() 方法
✅ 理解意图分类和分发逻辑
✅ 打开 workflows/shopping_agent.py，找到 handle() 方法
✅ 理解商品推荐的流程
✅ 画出一个请求的完整调用链
```

**验证**：能画出从 Android 到 Python AI 服务的完整请求流程图

---

### 第三周：理解 Agent 核心组件

**目标**：理解 Agent 的四个核心组件

**Day 1-2：RouterAgent（路由 Agent）**
```
✅ 阅读 workflows/router_agent.py
✅ 理解意图分类逻辑
✅ 理解怎么分发到不同 Agent
✅ 理解统一记忆写入
```

**Day 3-4：ShoppingAgent（购物导购）**
```
✅ 阅读 workflows/shopping_agent.py
✅ 理解四级搜索流水线
✅ 理解防幻觉校验
✅ 理解商品卡片构建
```

**Day 5-7：Orchestrator 和 ToolRegistry**
```
✅ 阅读 agent/orchestrator.py
✅ 理解编排流程和重试机制
✅ 阅读 tools/registry.py
✅ 理解工具注册和调用流程
```

**验证**：能画出 Agent 的执行流程图

---

### 第四周：理解多 Agent 协作

**目标**：理解多个 Agent 怎么协作

**Day 1-2：KnowledgeQAAgent**
```
✅ 阅读 workflows/knowledge_qa_agent.py
✅ 理解三级复杂度（L1/L2/L3）
✅ 理解检索增强流程
```

**Day 3-4：其他 Agent**
```
✅ 阅读 workflows/chitchat_agent.py —— 闲聊
✅ 阅读 workflows/reasoning_agent.py —— 复杂推理
✅ 阅读 workflows/admin_copilot_agent.py —— 管理助手
✅ 理解每个 Agent 的职责
```

**Day 5-7：意图分类和工具体系**
```
✅ 阅读 intent/classifier.py —— 意图分类器
✅ 阅读 tools/ 目录 —— 各个工具实现
✅ 理解 8 类意图怎么分类
✅ 理解 7 个工具怎么实现
```

**验证**：能画出完整的多 Agent 协作流程图

---

### 第五周：动手实践

**目标**：通过实践加深理解

**Day 1-2：添加日志**
```
✅ 在 shopping_agent.py 的 handle() 方法中添加 print 语句
✅ 运行项目，观察日志输出
✅ 理解代码执行顺序
```

**Day 3-4：修改参数**
```
✅ 修改知识检索的 top_k 参数（返回多少条结果）
✅ 修改 LLM 的 temperature 参数（回答的随机性）
✅ 观察效果变化
```

**Day 5-7：添加新功能**
```
✅ 尝试添加一个新的 Tool（比如天气查询工具）
✅ 尝试修改意图识别的逻辑
✅ 尝试添加一个新的 Agent 类型
```

**验证**：能独立完成一个小功能的添加

---

## 6. 核心概念详解

### 6.1 五层架构

```
RouterAgent（路由层）
├── 职责：识别意图，分发请求
├── 文件：workflows/router_agent.py
└── 类比：公司的前台，负责接待和分流

Orchestrator Layer（编排层）
├── 职责：协调整个 Agent 的执行流程
├── 文件：agent/orchestrator.py
└── 类比：项目经理，负责规划、分配任务、监督执行

Planner Layer（规划层）
├── 职责：决定做什么、怎么做
├── 文件：agent/planner.py
└── 类比：策划部门，负责制定方案

Executor Layer（执行层）
├── 职责：执行具体步骤
├── 文件：agent/executor.py
└── 类比：执行团队，负责按方案执行

State + Events Layer（状态与事件层）
├── 职责：管理状态、追踪事件
├── 文件：agent/state.py, agent/events.py
└── 类比：监控中心，负责记录和追踪
```

**为什么要分层？**
- 每层职责单一，容易理解和维护
- 可以独立测试和替换（比如换向量数据库只改 Tool 层）
- 简单请求可以直接分发，避免编排开销

---

### 6.2 多 Agent 协作

**RouterAgent（路由 Agent）**
```
用户问题："推荐跑鞋"
    ↓
RouterAgent 识别意图
    ↓
意图：shopping
    ↓
分发给 ShoppingAgent
```

**ShoppingAgent（购物导购 Agent）**
```
接收问题
    ↓
读取会话记忆 → "用户之前问过运动装备"
    ↓
四级搜索流水线 → 找到 5 款跑鞋
    ↓
LLM 生成推荐话术 → "根据您的需求，推荐以下跑鞋..."
    ↓
防幻觉校验 → 确认商品信息准确
    ↓
返回商品卡片
```

**为什么要分多个 Agent？**
- 不同任务的处理逻辑差异很大（闲聊 2 秒，商品推荐 4 秒）
- 分开后每个 Agent 只做一件事，可以独立优化
- ShoppingAgent 有 1700+ 行代码，混在一起根本无法维护

---

### 6.3 四级搜索流水线

**问题背景**：用户说"推荐跑鞋"，如果简单拆字匹配，"跑"和"鞋"会被分开匹配，导致篮球鞋也混入结果。

**四级搜索流水线**：

```
Step 1: LLM 提取排除词
  用户说"不要优衣库" → 提取 ["优衣库"]
  后续搜索过滤掉匹配商品

Step 2: LLM + 正则提取搜索关键词
  用户说"推荐跑鞋" → 提取 ["跑鞋"]

Step 3: 三级优先级匹配
  精确匹配（最高优先级）: "跑鞋" → 只匹配"跑步鞋"，不展开
  泛词展开（中等优先级）: "鞋子" → ["篮球鞋","跑步鞋","徒步鞋","运动鞋"]
  拆字匹配（最低优先级）: 中文单字拆分模糊搜索

Step 4: SQL 参数化搜索 + 品类过滤 + 排除词过滤

Step 5: 后处理 → 去重 + 状态过滤 + 加权排序（评分60% + 销量40%）
```

**为什么要四级？**
- 精确匹配确保结果准确（"跑鞋"不会返回篮球鞋）
- 泛词展开确保结果全面（"鞋子"能找到所有类型的鞋）
- 排除词让用户可以说"不要"来过滤
- 加权排序确保高质量商品排在前面

---

### 6.4 三层防幻觉机制

**问题背景**：LLM 可能编造不存在的商品、价格、评分等数据。

**三层防幻觉**：

```
Layer 1: Prompt 约束
  LLM Prompt 明确指令：
  "不要编造价格/评分/销量数据"
  "不要推荐商品信息中没有的商品"
  "所有商品信息必须来自提供的数据"

Layer 2: 输出校验
  validateProductCards() 函数：
  用 product_id 查数据库
  比对价格/标题/状态
  不一致则用数据库数据覆盖

Layer 3: 参数化查询
  SQL 从 f-string 改为参数化查询
  防止恶意输入和 SQL 注入
```

**为什么要三层？**
- Prompt 约束是第一道防线，但 LLM 可能不遵守
- 输出校验是第二道防线，确保数据准确
- 参数化查询是第三道防线，防止安全漏洞

---

## 7. 代码阅读指引

### 7.1 从入口开始

**第一步：找到 Python AI 服务的入口**
```
backend/python-service/main.py
```

**第二步：找到 Agent 的入口**
```
backend/python-service/api/routes.py
    ↓
找到处理用户消息的接口
    ↓
调用 workflows/router_agent.py 的 handle() 方法
```

**第三步：理解 RouterAgent 的 handle() 方法**
```python
def handle(self, message, ...):
    # 1. 意图分类
    intent_result = self.classifier.classify(message)

    # 2. 根据意图分发
    if intent_result.intent == IntentType.SHOPPING:
        agent = ShoppingAgent()
    elif intent_result.intent == IntentType.KNOWLEDGE:
        agent = KnowledgeQAAgent()
    elif intent_result.intent == IntentType.CHITCHAT:
        agent = ChitChatAgent()
    # ...

    # 3. 调用子 Agent
    response = agent.handle(message, ...)

    # 4. 统一记忆写入
    self._write_memory(message, response, ...)

    return response
```

---

### 7.2 跟着请求走

**一个商品推荐请求的完整流程**：

```
1. 用户在 Android 端输入："推荐跑鞋"
   ↓
2. Android 发送 HTTP 请求到 Java 后端
   ↓
3. Java ChatController 接收请求
   ↓
4. Java AiService 调用 Python /api/chat/stream
   ↓
5. Python api/routes.py 接收请求
   ↓
6. workflows/router_agent.py 的 handle() 方法开始执行
   ↓
7. intent/classifier.py 识别意图 → shopping
   ↓
8. 分发给 workflows/shopping_agent.py
   ↓
9. ShoppingAgent 执行：
   - 读取会话记忆
   - FAISS 向量检索
   - 四级搜索流水线
   - LLM 生成推荐话术
   - 防幻觉校验
   - 构建商品卡片
   ↓
10. SSE 流式返回 → Java 透传 → Android 逐字渲染
```

---

### 7.3 关键代码片段解读

**RouterAgent 怎么分发请求？**
```python
def handle(self, message, ...):
    # 1. 识别意图
    intent_result = self.classifier.classify(message)

    # 2. 根据意图分发
    if intent_result.intent == IntentType.SHOPPING:
        agent = ShoppingAgent()
    elif intent_result.intent == IntentType.KNOWLEDGE:
        agent = KnowledgeQAAgent()
    elif intent_result.intent == IntentType.CHITCHAT:
        agent = ChitChatAgent()
    # ...

    # 3. 调用子 Agent
    response = agent.handle(message, ...)
    return response
```

**ToolRegistry 怎么调用工具？**
```python
def invoke_tool(self, tool_name, parameters, run_id):
    # 1. 验证参数
    tool.validate_input(parameters)

    # 2. 开始追踪
    tool_call_id = tracker.start_tool_call()

    # 3. 超时控制
    future = self._executor.submit(tool.execute, parameters)
    result = future.result(timeout=timeout_sec)

    # 4. 结束追踪
    tracker.end_tool_call(tool_call_id)

    return result
```

**ShoppingAgent 怎么搜索商品？**
```python
def _search_products(self, message, memory):
    # Step 1: 提取排除词
    exclude_words = self._extract_exclude_words(message)

    # Step 2: 提取关键词
    keywords = self._extract_keywords(message)

    # Step 3: 三级优先级匹配
    # 精确匹配 → 泛词展开 → 拆字匹配

    # Step 4: SQL 搜索 + 过滤

    # Step 5: 后处理 + 排序
    return products
```

---

## 8. 动手实践建议

### 8.1 第一个实践：添加日志

**目标**：通过添加日志理解代码执行顺序

**步骤**：
1. 打开 `backend/python-service/workflows/shopping_agent.py`
2. 在 `handle()` 方法的关键位置添加 print 语句
3. 运行项目，问一个问题
4. 观察日志输出

**示例代码**：
```python
def handle(self, message, ...):
    print(f"[ShoppingAgent] 开始处理: {message}")

    memory = self._read_memory(conversation_id)
    print(f"[ShoppingAgent] 读取记忆: {len(memory)} 条")

    products = self._search_products(message, memory)
    print(f"[ShoppingAgent] 搜索结果: {len(products)} 个商品")

    recommendation = self._generate_recommendation(message, products)
    print(f"[ShoppingAgent] 生成推荐: {recommendation[:50]}...")

    print(f"[ShoppingAgent] 处理完成")
```

---

### 8.2 第二个实践：修改参数

**目标**：通过修改参数理解参数的作用

**实践 1：修改检索数量**
```
文件：backend/python-service/workflows/shopping_agent.py
参数：搜索结果数量
默认值：5
尝试：改成 3 或 10，观察效果变化
```

**实践 2：修改 LLM 温度**
```
文件：backend/python-service/core/llm.py
参数：temperature（回答的随机性）
默认值：0.7
尝试：改成 0.1（更确定）或 1.0（更随机），观察效果变化
```

---

### 8.3 第三个实践：添加新工具

**目标**：理解工具的抽象和注册机制

**步骤**：
1. 创建新文件 `backend/python-service/tools/weather_tool.py`
2. 继承 Tool 基类
3. 实现 execute() 方法
4. 在 registry.py 中注册工具
5. 在 Agent 中添加使用场景

**示例代码**：
```python
# tools/weather_tool.py
from tools.base import Tool

class WeatherTool(Tool):
    name = "weather"
    description = "查询天气信息"

    def execute(self, parameters):
        city = parameters.get("city", "北京")
        # 这里可以调用真实的天气 API
        return {"city": city, "weather": "晴", "temperature": "25°C"}

# tools/registry.py 中注册
from tools.weather_tool import WeatherTool
tool_registry.register_tool(WeatherTool())
```

---

## 9. 常见问题与解决方案

### 9.1 代码看不懂怎么办？

**策略 1：从入口开始**
```
不要试图一次看懂所有代码
先找到入口（main.py → api/routes.py → router_agent.py）
跟着请求走，理解主流程
```

**策略 2：画图辅助**
```
画出调用链：A 调用 B，B 调用 C
画出数据流：数据从哪里来，到哪里去
画出状态机：状态怎么变化
```

**策略 3：添加日志**
```
在关键位置添加 print 语句
运行代码，观察执行顺序
通过日志理解代码逻辑
```

---

### 9.2 项目跑不起来怎么办？

**策略 1：检查环境**
```bash
# 检查 Java 版本
java -version

# 检查 Python 版本
python --version

# 检查 Node.js 版本
node --version

# 检查 MySQL 是否运行
mysql -u root -p

# 检查 Redis 是否运行
redis-cli ping
```

**策略 2：查看错误日志**
```
错误日志通常会告诉你哪里出错了
重点关注：缺什么模块、连不上数据库、配置错误
```

**策略 3：搜索解决方案**
```
把错误信息复制到搜索引擎
GitHub Issues 中可能有人遇到过同样的问题
Stack Overflow 上可能有解决方案
```

---

### 9.3 概念太多记不住怎么办？

**策略 1：先理解核心概念**
```
必须理解：Agent、RAG、Tool、意图分类
可以跳过：向量数据库细节、LLM 原理、设计模式
```

**策略 2：通过代码理解概念**
```
不要死记概念，通过代码理解
比如：看完 router_agent.py，自然理解什么是"路由"
```

**策略 3：做笔记**
```
把重要概念记下来
用自己的话解释概念
画图辅助理解
```

---

## 10. 学习资源推荐

### 10.1 Java/Spring Boot 学习

**入门**：
- [Spring Boot 官方文档](https://spring.io/projects/spring-boot)
- [Spring Boot 教程](https://www.w3schools.com/spring-boot/)

**进阶**：
- [Spring Boot 实战](https://www.manning.com/books/spring-boot-in-action)
- [Spring 微服务实战](https://www.manning.com/books/spring-microservices-in-action)

---

### 10.2 Python/FastAPI 学习

**入门**：
- [Python 官方教程](https://docs.python.org/3/tutorial/)
- [FastAPI 官方文档](https://fastapi.tiangolo.com/)

**进阶**：
- [Fluent Python](https://www.oreilly.com/library/view/fluent-python-2nd/9781492056348/)
- [FastAPI 实战](https://www.manning.com/books/fastapi-in-action)

---

### 10.3 Android/Kotlin 学习

**入门**：
- [Kotlin 官方教程](https://kotlinlang.org/docs/home.html)
- [Jetpack Compose 官方教程](https://developer.android.com/jetpack/compose/tutorial)

**进阶**：
- [Android 开发者文档](https://developer.android.com/guide)
- [Jetpack Compose 最佳实践](https://developer.android.com/jetpack/compose/best-practices)

---

### 10.4 AI/Agent 学习

**入门**：
- [什么是 RAG](https://www.pinecone.io/learn/retrieval-augmented-generation/)
- [什么是 Agent](https://www.pinecone.io/learn/series/langchain/langchain-agents/)
- [LLM 基础概念](https://www.promptingguide.ai/)

**进阶**：
- [LangChain 官方文档](https://docs.langchain.com/)
- [Lilian Weng 的 Agent 博客](https://lilianweng.github.io/posts/2023-06-23-agent/)
- [ReAct 论文](https://arxiv.org/abs/2210.03629)

---

### 10.5 项目实战

**推荐项目**：
- [Dify](https://github.com/langgenius/dify) — 生产级 Agent 平台
- [LangChain](https://github.com/langchain-ai/langchain) — Agent 框架
- [AutoGen](https://github.com/microsoft/autogen) — 多 Agent 框架

**学习方式**：
- 先跑通项目
- 阅读核心代码
- 尝试修改和扩展
- 对比不同项目的实现

---

## 附录：学习检查清单

### 第一周检查清单
```
✅ 能成功启动项目
✅ 能用 Android 端问问题并看到商品推荐
✅ 能看到管理端的 Agent 执行记录
✅ 能画出项目的三端架构图
✅ 能向别人解释"这个项目是干什么的"
```

### 第二周检查清单
```
✅ 能找到 Android 端发送请求的代码
✅ 能找到 Java 后端处理请求的接口
✅ 能找到 Python AI 服务的入口
✅ 能画出请求的完整调用链
```

### 第三周检查清单
```
✅ 理解 RouterAgent 怎么分发请求
✅ 理解 ShoppingAgent 怎么推荐商品
✅ 理解 Orchestrator 怎么编排流程
✅ 理解 ToolRegistry 怎么管理工具
✅ 能画出 Agent 的执行流程图
```

### 第四周检查清单
```
✅ 理解 KnowledgeQAAgent 的三级复杂度
✅ 理解各专业 Agent 的职责
✅ 理解意图分类怎么实现
✅ 理解 7 个工具怎么工作
✅ 能画出多 Agent 协作流程图
```

### 第五周检查清单
```
✅ 能通过添加日志理解代码执行顺序
✅ 能修改参数观察效果变化
✅ 能添加一个简单的新工具
✅ 能向别人解释项目的核心设计
```

---

> **文档版本**：v1.0
> **更新日期**：2026-06-11
> **目标读者**：有基础但没有项目经验的新手
> **配合使用**：README.md（项目说明） + ShopAgent-X学习指南.md（进阶） + 本指南（入门）
