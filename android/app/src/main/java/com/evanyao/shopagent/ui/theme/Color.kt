package com.evanyao.shopagent.ui.theme

import androidx.compose.ui.graphics.Color

// 品牌主色 - 温暖橙色系
val Primary = Color(0xFFFF6B35)
val PrimaryLight = Color(0xFFFF8F62)
val PrimaryDark = Color(0xFFE55A25)
val PrimaryPale = Color(0xFFFFF0EB) // 极浅橙，用于选中态底色、标签背景

// 强调色
val AccentGold = Color(0xFFF0A500) // 金色，用于评分、星星

// 背景色 - 暖色体系
val Background = Color(0xFFFFF8F3) // 暖白，带微橙调
val Surface = Color(0xFFFFFAF6) // 卡片/弹窗，微暖白
val SurfaceVariant = Color(0xFFF5EDE8) // 分割线、边框、禁用背景
val SurfaceContainer = Color(0xFFFFF3EC) // 浅暖容器色

// 文字色 - 深棕灰替代纯黑
val TextPrimary = Color(0xFF3D2C1E) // 深棕灰
val TextSecondary = Color(0xFF8C7A6B) // 中棕灰
val TextHint = Color(0xFFBDB0A5) // 浅棕灰
val TextOnPrimary = Color(0xFFFFFFFF) // 主色上的文字

// 边框/分割线
val DividerWarm = Color(0xFFEDE4DD) // 暖色分割线
val BorderWarm = Color(0xFFE5D9D0) // 暖色边框

// 功能色
val Success = Color(0xFF00B894)
val Warning = Color(0xFFFDCB6E)
val Error = Color(0xFFE17055)
val Info = Color(0xFF74B9FF)

// 评分色
val StarColor = Color(0xFFFFD700)

// 价格色
val PriceColor = Color(0xFFE17055)
