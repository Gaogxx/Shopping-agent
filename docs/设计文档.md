# ShopAgent-X 项目设计文档

> 基于 RAG 的多模态电商智能导购 AI Agent 系统详细设计

---

## 目录

1. [系统架构](#1-系统架构)
2. [技术栈](#2-技术栈)
3. [依赖环境](#3-依赖环境)
4. [目录结构](#4-目录结构)
5. [数据库设计](#5-数据库设计)
6. [配置说明](#6-配置说明)
7. [核心模块设计](#7-核心模块设计)
8. [关键问题解决方案](#8-关键问题解决方案)
9. [API 接口清单](#9-api-接口清单)
10. [开发过程中遇到的问题与解决方案](#10-开发过程中遇到的问题与解决方案)
11. [项目创新点](#11-项目创新点)

---

## 1. 系统架构

### 1.1 整体架构

三端分离架构：**Android App** → **Java Backend** → **Python AI Service**

```
┌─────────────────────────────────────────────────────────────┐
│                     客户端层                                  │
│  ┌──────────────────┐  ┌──────────────────┐                 │
│  │  Android App     │  │  React 管理端     │                 │
│  │  Kotlin Compose  │  │  Vite + React 18 │                 │
│  │  :端口 自动分配   │  │  :5173           │                 │
│  └────────┬─────────┘  └────────┬─────────┘                 │
└───────────┼─────────────────────┼───────────────────────────┘
            │ HTTP/SSE            │ HTTP
            ▼                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  Java 后端 (Spring Boot 3.2)                 │
│                    端口 :8888                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ Auth API │ │ Chat API │ │Product API│ │ Admin API│       │
│  │ JWT 认证  │ │ SSE 流式  │ │ 商品 CRUD │ │ 管理后台  │       │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘       │
│       └────────────┼───────────┼─────────────┘              │
│                    ▼           ▼                             │
│              ┌──────────────────┐                           │
│              │   AiService      │                           │
│              │  REST 调用 Python │                           │
│              └────────┬─────────┘                           │
└───────────────────────┼─────────────────────────────────────┘
                        │ HTTP (REST)
                        ▼
┌─────────────────────────────────────────────────────────────┐
│               Python AI 服务 (FastAPI)                       │
│                    端口 :8000                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              RouterAgent (意图分类 + 分发)             │    │
│  │    ┌──────────┬──────────┬──────────┬──────────┐    │    │
│  │    │Shopping  │Knowledge │ChitChat  │Reasoning │    │    │
│  │    │Agent     │QAAgent   │Agent     │Agent     │    │    │
│  │    └────┬─────┘────┬─────┘──────────┘──────────┘    │    │
│  │         │          │                                │    │
│  │    ┌────▼──────────▼────┐  ┌───────────────────┐   │    │
│  │    │  五层编排框架       │  │  ToolRegistry     │   │    │
│  │    │  Orchestrator      │  │  7 个已注册工具    │   │    │
│  │    │  → Planner         │  └───────────────────┘   │    │
│  │    │  → Executor        │                          │    │
│  │    │  → State + Events  │                          │    │
│  │    └────────────────────┘                          │    │
│  └─────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
            ┌────────────┼────────────┐
            ▼            ▼            ▼
      ┌──────────┐ ┌──────────┐ ┌──────────┐
      │ MySQL 8  │ │ Redis 7  │ │ FAISS    │
      │ 25 张表   │ │ 缓存+会话 │ │ 向量检索  │
      │ :3306    │ │ :6379    │ │ 本地索引  │
      └──────────┘ └──────────┘ └──────────┘
```

### 1.2 请求链路

**普通对话流程：**
```
App 发送消息 → Java ChatController.sendMessage()
  → 调用 Python /api/chat/stream (SSE 流式)
  → RouterAgent 意图分类 → 分发到子 Agent
  → 子 Agent 处理（可能调用 Tool）
  → SSE 逐 token 返回 → Java 透传 → App 逐字渲染
```

**商品推荐流程：**
```
用户: "推荐一款洗面奶"
  → RouterAgent: 意图=shopping → ShoppingAgent
  → ShoppingAgent:
    1. 读取会话记忆
    2. FAISS 向量检索相关商品知识
    3. MySQL 四级搜索流水线（排除词→关键词→精确匹配→泛词展开）
    4. LLM 生成推荐话术
    5. 构建商品卡片 JSON
  → SSE 返回: routed → token(逐字) → product_cards → end
```

### 1.3 Agent 协作拓扑

```
RouterAgent（中央路由，8 类意图，88.9% 准确率）
  │
  ├── ShoppingAgent      → 商品推荐 / 购物车管理 / 反选排除
  ├── KnowledgeQAAgent   → L1 直答 / L2 检索 / L3 编排
  ├── ChitChatAgent      → 闲聊（带会话记忆）
  ├── ReasoningAgent     → 复杂推理
  └── AdminCopilotAgent  → 运营分析 / 知识巡检
        ├── OpsAgent
        └── InspectionAgent
```

---

## 2. 技术栈

### 2.1 Android 客户端

| 技术 | 版本 | 用途 |
|------|------|------|
| Kotlin | 2.2.10 | 开发语言 |
| Jetpack Compose | BOM 2026.02.01 | 声明式 UI 框架 |
| Material3 | - | Material Design 3 组件库 |
| Koin | 3.5.3 | 依赖注入（替代 Hilt） |
| Retrofit | 2.9.0 | HTTP 客户端 |
| OkHttp | 4.12.0 | 网络层（含 SSE 支持） |
| OkHttp SSE | 4.12.0 | Server-Sent Events 流式接收 |
| Coil | 2.5.0 | 图片加载 |
| Navigation Compose | 2.7.7 | 页面路由导航 |
| DataStore | 1.0.0 | 本地持久化（Token 存储） |
| MediaRecorder | Android SDK | 语音录制 |
| minSdk / targetSdk | 24 / 36 | Android 7.0 ~ 15 兼容 |

### 2.2 Java 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.2.3 | 应用框架 |
| Spring Security | - | 安全认证框架 |
| JWT (jjwt) | 0.11.5 | Token 认证 |
| MyBatis Plus | 3.5.5 | ORM 框架 |
| Spring Data Redis | - | Redis 客户端 |
| Caffeine | 3.1.8 | 本地缓存（二级缓存 L1） |
| WebFlux | - | SSE 流式响应支持 |
| MySQL Connector | - | 数据库驱动 |
| Hutool | 5.8.25 | 工具库 |
| Aliyun SMS SDK | 2.2.1 | 短信验证码 |
| Qiniu SDK | 7.13.0 | 七牛云对象存储 |
| SpringDoc OpenAPI | 2.3.0 | API 文档 (Swagger) |
| Lombok | - | 代码简化 |

### 2.3 Python AI 服务

| 技术 | 版本 | 用途 |
|------|------|------|
| FastAPI | 0.110+ | Web 框架 + SSE |
| Uvicorn | 0.27.1+ | ASGI 服务器 |
| LangChain | 0.1.11+ | LLM 应用框架 |
| LangChain OpenAI | 0.1.0+ | OpenAI 兼容接口 |
| 豆包 Seed 2.0 Lite | - | 主力 LLM（字节跳动） |
| DashScope | 1.14+ | 阿里云 AI API（Embedding） |
| FAISS (CPU) | 1.8.0+ | 本地向量数据库 |
| sentence-transformers | 2.5.1+ | 文本 Embedding 模型 |
| MySQL Connector | 8.2.0+ | 数据库直连 |
| Redis | 5.0.0+ | 缓存客户端 |
| Pillow | 10.3.0+ | 图像处理 |
| Pytesseract | 0.3.10+ | OCR 文字提取（fallback） |
| Cohere | 4.0.0+ | Rerank 重排序（可选） |

### 2.4 前端管理端

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.2.0 | UI 框架 |
| Vite | 5.1.0 | 构建工具 |
| React Router DOM | 6.22.0 | 路由管理 |
| Axios | 1.6.7 | HTTP 客户端 |
| ECharts | 6.0.0 | 数据可视化图表 |

### 2.5 基础设施

| 技术 | 版本 | 用途 |
|------|------|------|
| MySQL | 8.0 | 业务数据库（25 张表） |
| Redis | 7 (Alpine) | 缓存 + 会话管理 |
| Docker Compose | 3.8 | 一键启动 MySQL + Redis |

---

## 3. 依赖环境

### 3.1 开发环境要求

| 环境 | 最低版本 | 说明 |
|------|---------|------|
| JDK | 17 | Spring Boot 3.x 要求 |
| Python | 3.10+ | LangChain / FAISS 要求 |
| Node.js | 18+ | Vite 构建要求 |
| MySQL | 8.0 | JSON 字段、utf8mb4 支持 |
| Redis | 7.0 | 缓存和会话存储 |
| Docker | 20+ | 运行 MySQL + Redis 容器 |
| Android Studio | Hedgehog+ | 编译 Android 客户端 |
| Gradle | 8.x | Android 构建工具 |

### 3.2 环境变量

**Python 服务 (.env)：**

| 变量 | 必填 | 说明 |
|------|------|------|
| `DOUBAO_API_KEY` | 是 | 豆包大模型 API Key |
| `DOUBAO_BASE_URL` | 是 | 豆包 API 地址 |
| `DOUBAO_MODEL` | 是 | 豆包模型端点 ID |
| `DASHSCOPE_API_KEY` | 否 | 阿里云 DashScope（Embedding） |
| `USE_MILVUS` | 否 | 是否使用 Milvus（默认 false，用 FAISS） |
| `RERANKER_TYPE` | 否 | Rerank 策略：bge/cohere/simple/none |
| `TESSERACT_PATH` | 否 | Tesseract OCR 路径 |

**Java 后端 (application.yml)：**

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `SERVER_PORT` | 8888 | 服务端口 |
| `DB_HOST` / `DB_PORT` | localhost:3306 | MySQL 地址 |
| `DB_USER` / `DB_PASSWORD` | root / 123456 | MySQL 凭据 |
| `REDIS_HOST` / `REDIS_PORT` | localhost:6379 | Redis 地址 |
| `AI_SERVICE_URL` | http://127.0.0.1:8000/api | Python AI 服务地址 |
| `JWT_SECRET` | (内置默认值) | JWT 签名密钥 |
| `JWT_EXPIRATION` | 3600000 | Token 过期时间（1小时） |
| `ALIYUN_SMS_*` | (空) | 阿里云短信配置（未配置则模拟模式） |
| `QINIU_*` | (空) | 七牛云存储配置 |

**Android 客户端：**

| 变量 | 值 | 说明 |
|------|------|------|
| `BASE_URL` | BuildConfig 注入 | Java 后端地址，build.gradle.kts 中配置 |

---

## 4. 目录结构

```
ShopAgent-X/
├── android/                              # Android 客户端
│   ├── app/src/main/java/com/evanyao/shopagent/
│   │   ├── MainActivity.kt               # 主 Activity
│   │   ├── SplashActivity.kt             # 启动页
│   │   ├── ShopAgentApp.kt               # Application（Koin 初始化）
│   │   ├── navigation/                   # 路由导航
│   │   │   ├── NavGraph.kt              # 主导航图（所有页面路由）
│   │   │   ├── Screen.kt                # 页面路由定义（sealed class）
│   │   │   └── BottomNavItem.kt         # 底部导航栏定义
│   │   ├── ui/
│   │   │   ├── screens/
│   │   │   │   ├── auth/                # 登录/注册/画像引导/改密码
│   │   │   │   ├── chat/                # 对话页（核心页面）
│   │   │   │   ├── cart/                # 购物车
│   │   │   │   ├── product/             # 商品列表/详情
│   │   │   │   ├── order/               # 订单列表/详情/结算
│   │   │   │   └── profile/             # 个人中心/收藏/历史/地址/设置
│   │   │   ├── components/              # 通用组件
│   │   │   │   ├── MessageBubble.kt     # 消息气泡（含商品卡片、确认卡片）
│   │   │   │   ├── ProductCardInChat.kt # 对话中的商品卡片
│   │   │   │   ├── RecommendSection.kt  # 推荐问题区域
│   │   │   │   └── ...
│   │   │   └── theme/                   # Material3 主题（暖橙色系）
│   │   ├── viewmodel/                   # 9 个 ViewModel
│   │   │   ├── ChatViewModel.kt         # 对话核心逻辑（SSE 流式）
│   │   │   ├── AuthViewModel.kt         # 认证
│   │   │   ├── CartViewModel.kt         # 购物车
│   │   │   ├── ProductViewModel.kt      # 商品
│   │   │   ├── OrderViewModel.kt        # 订单
│   │   │   └── ...
│   │   ├── data/
│   │   │   ├── network/
│   │   │   │   ├── RetrofitClient.kt    # Retrofit 配置
│   │   │   │   ├── SseClient.kt         # SSE 流式客户端
│   │   │   │   ├── AuthInterceptor.kt   # JWT 拦截器
│   │   │   │   └── api/                 # API 接口定义（6 个）
│   │   │   ├── repository/              # 数据仓库层（6 个）
│   │   │   ├── model/                   # 数据模型（17 个）
│   │   │   └── TokenManager.kt          # Token 管理
│   │   ├── di/
│   │   │   ├── AppModule.kt             # Koin 模块定义
│   │   │   └── NetworkModule.kt         # 网络层 DI
│   │   └── util/
│   │       └── AudioRecorder.kt         # 录音工具（MediaRecorder）
│   └── app/build.gradle.kts             # Android 构建配置
│
├── backend/
│   ├── src/main/java/com/demo/aiknowledge/
│   │   ├── controller/                  # 13 个 REST 控制器
│   │   │   ├── AuthController.java      # 认证（登录/注册/验证码）
│   │   │   ├── ChatController.java      # 对话（SSE 流式/图片识别/语音识别）
│   │   │   ├── ProductController.java   # 商品
│   │   │   ├── CartController.java      # 购物车
│   │   │   ├── OrderController.java     # 订单
│   │   │   ├── RecommendController.java # 推荐
│   │   │   ├── KnowledgeController.java # 知识库管理
│   │   │   ├── AgentController.java     # Agent 运行记录
│   │   │   ├── CacheController.java     # 缓存管理
│   │   │   ├── NoticeController.java    # 通知公告
│   │   │   └── admin/                   # 10 个管理端控制器
│   │   │       ├── DashboardController.java
│   │   │       ├── AdminProductController.java
│   │   │       ├── AdminOrderController.java
│   │   │       ├── AdminConversationController.java
│   │   │       ├── AgentAdminController.java
│   │   │       └── ...
│   │   ├── service/                     # 25 个 Service 实现
│   │   ├── entity/                      # 25 个数据实体
│   │   ├── mapper/                      # MyBatis Plus Mapper
│   │   ├── config/                      # 配置类
│   │   │   ├── SecurityConfig.java      # Spring Security 配置
│   │   │   ├── JwtConfig.java           # JWT 配置
│   │   │   ├── CacheConfig.java         # Redis + Caffeine 二级缓存
│   │   │   └── WebConfig.java           # CORS + 静态资源
│   │   └── common/                      # 通用工具（Result、异常处理）
│   ├── src/main/resources/
│   │   ├── application.yml              # 主配置文件
│   │   └── mapper/                      # MyBatis XML 映射
│   ├── sql/init.sql                     # 数据库初始化（25 张表 + 示例数据）
│   ├── pom.xml                          # Maven 依赖
│   └── frontend/                        # React 管理端
│       ├── src/
│       │   ├── App.jsx                  # 路由定义
│       │   ├── api/                     # API 封装（admin.js 等）
│       │   ├── components/              # 通用组件
│       │   └── pages/
│       │       ├── Chat.jsx             # 普通用户对话
│       │       └── admin/               # 管理端页面（12 个）
│       │           ├── AdminDashboard.jsx    # 管理端布局
│       │           ├── Dashboard.jsx         # 数据大屏
│       │           ├── ProductManagement.jsx # 商品管理
│       │           ├── OrderManagement.jsx   # 订单管理
│       │           ├── ConversationManagement.jsx
│       │           ├── KnowledgeManagement.jsx
│       │           ├── KnowledgeInspection.jsx
│       │           ├── AgentRunManagement.jsx
│       │           └── ...
│       └── package.json
│
├── backend/python-service/              # Python AI 服务
│   ├── main.py                          # FastAPI 入口
│   ├── api/
│   │   └── routes.py                    # 所有 API 路由定义
│   ├── agent/                           # 五层编排框架
│   │   ├── orchestrator.py             # 编排器（创建 state → plan → execute）
│   │   ├── planner.py                  # 规划器（生成执行步骤）
│   │   ├── executor.py                 # 执行器（按步骤分发执行）
│   │   ├── state.py                    # 状态管理（run_id / trace_id）
│   │   ├── events.py                   # 事件总线 + MetricsCollector
│   │   ├── policies.py                 # 策略（Guardrails / Retry）
│   │   └── memory_agent.py             # 记忆 Agent
│   ├── workflows/                       # 9 个专业 Agent
│   │   ├── base_agent.py               # Agent 基类
│   │   ├── agent_response.py           # 统一返回格式
│   │   ├── router_agent.py             # 中央路由（意图分类 + 分发）
│   │   ├── shopping_agent.py           # 购物导购（核心，1700+ 行）
│   │   ├── knowledge_qa_agent.py       # 知识问答（L1/L2/L3）
│   │   ├── chitchat_agent.py           # 闲聊
│   │   ├── reasoning_agent.py          # 复杂推理
│   │   ├── retrieval_agent.py          # 检索 Agent
│   │   ├── admin_copilot_agent.py      # 管理助手
│   │   ├── ops_agent.py                # 运营分析
│   │   └── inspection_agent.py         # 知识巡检
│   ├── tools/                           # 7 个 AI 工具 + 注册体系
│   │   ├── base.py                     # 工具基类
│   │   ├── registry.py                 # ToolRegistry 单例
│   │   ├── execution.py                # 执行跟踪（TTL + 容量上限）
│   │   ├── knowledge_search.py         # 知识库语义检索
│   │   ├── question_rewrite.py         # 问题改写 + 画像感知
│   │   ├── rerank.py                   # 检索结果重排序
│   │   ├── memory_read.py              # 会话记忆读取
│   │   ├── memory_write.py             # 会话记忆写入
│   │   ├── ocr_extract.py              # 图片文字提取
│   │   ├── cart_tool.py                # 购物车 CRUD
│   │   ├── citation.py                 # 引用生成
│   │   └── doc_summary.py              # 文档摘要
│   ├── core/                            # 核心基础设施
│   │   ├── config.py                   # 配置管理（环境变量读取）
│   │   ├── llm.py                      # LLM 服务（豆包/DashScope）
│   │   ├── vector_store.py             # 向量存储（FAISS/Milvus）
│   │   ├── mysql_client.py             # MySQL 客户端
│   │   ├── redis_client.py             # Redis 客户端
│   │   ├── reranker.py                 # Rerank 服务
│   │   ├── text_splitter.py            # 文本切分器
│   │   └── parser.py                   # 文档解析器
│   ├── intent/
│   │   └── classifier.py               # 意图分类器
│   └── requirements.txt                # Python 依赖
│
├── data/ecommerce_agent_dataset/        # 数据集（4 品类 × 25 商品 = 100 条）
├── docs/                                # 文档
│   ├── 设计文档.md                      # 本文档
│   └── screenshots/                     # 截图 + 演示视频
├── docker-compose.yml                   # MySQL + Redis 容器编排
└── README.md                            # 项目说明
```

---

## 5. 数据库设计

### 5.1 表清单（25 张表）

| 序号 | 表名 | 说明 | 分类 |
|------|------|------|------|
| 1 | `user` | 普通用户（App 端消费者） | 用户体系 |
| 2 | `admin` | 后台管理员 | 用户体系 |
| 3 | `category` | 商品品类（4 个一级分类） | 电商商品 |
| 4 | `product` | 商品主表 | 电商商品 |
| 5 | `product_sku` | 商品 SKU（规格/价格） | 电商商品 |
| 6 | `product_image` | 商品图片 | 电商商品 |
| 7 | `product_review` | 商品评价（RAG 语料） | 电商商品 |
| 8 | `product_faq` | 商品 FAQ（RAG 语料） | 电商商品 |
| 9 | `conversation` | 对话会话 | 对话体系 |
| 10 | `message` | 对话消息 | 对话体系 |
| 11 | `conversation_context` | 对话上下文（摘要/偏好） | 对话体系 |
| 12 | `agent_run` | Agent 运行记录 | Agent 监控 |
| 13 | `agent_step` | Agent 执行步骤 | Agent 监控 |
| 14 | `tool_call` | 工具调用记录 | Agent 监控 |
| 15 | `recommendation_log` | 推荐日志（含用户反馈） | 数据分析 |
| 16 | `user_browse_history` | 浏览记录 | 用户行为 |
| 17 | `user_cart` | 购物车 | 用户行为 |
| 18 | `user_favorite` | 收藏 | 用户行为 |
| 19 | `qa_log` | 问答日志 | 数据分析 |
| 20 | `qa_unanswered` | 未命中问题 | 数据分析 |
| 21 | `knowledge_doc` | 知识库文档 | 知识库 |
| 22 | `knowledge_chunk` | 文档切片 | 知识库 |
| 23 | `notice` | 通知公告 | 系统 |
| 24 | `orders` | 订单主表 | 订单体系 |
| 25 | `order_item` | 订单明细 | 订单体系 |

### 5.2 核心表关系

```
user ──1:N── conversation ──1:N── message
  │                                  │
  ├──1:N── user_cart                 ├── recommendation_log（反馈）
  ├──1:N── user_favorite
  ├──1:N── user_browse_history
  └──1:N── orders ──1:N── order_item ──→ product

category ──1:N── product ──1:N── product_sku
                        ├──1:N── product_image
                        ├──1:N── product_review（RAG 语料）
                        └──1:N── product_faq（RAG 语料）

knowledge_doc ──1:N── knowledge_chunk（向量化存储）

agent_run ──1:N── agent_step ──1:N── tool_call
```

---

## 6. 配置说明

### 6.1 快速启动

```bash
# 1. 启动基础服务
docker-compose up -d    # MySQL (3306) + Redis (6379)

# 2. 启动 Python AI 服务
cd backend/python-service
python -m venv venv && venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env    # 填入 DOUBAO_API_KEY
python main.py          # 端口 8000

# 3. 启动 Java 后端
cd backend
mvn spring-boot:run     # 端口 8888

# 4. 启动 React 管理端（可选）
cd backend/frontend
npm install && npm run dev  # 端口 5173

# 5. 编译 Android 客户端
# Android Studio 打开 android/ 目录，Gradle 同步后编译运行
```

### 6.2 Docker Compose 配置

| 服务 | 镜像 | 端口 | 数据卷 |
|------|------|------|--------|
| MySQL | mysql:8.0 | 3306 | mysql_data:/var/lib/mysql |
| Redis | redis:7-alpine | 6379 | redis_data:/data |

- MySQL 启动时自动执行 `backend/sql/init.sql` 初始化表结构和数据
- 默认 root 密码：`123456`
- 数据库名：`shop_agent_db`，字符集 `utf8mb4`

### 6.3 二级缓存配置

Java 后端采用 **Redis + Caffeine 二级缓存**：

```
请求 → Caffeine (L1, 本地内存, ms级) → Redis (L2, 分布式, 10ms级) → MySQL
```

- L1 Caffeine：热点数据本地缓存，减少网络开销
- L2 Redis：分布式缓存，跨实例共享
- 缓存失效：写操作时同时清除 L1 + L2

### 6.4 向量存储配置

默认使用 **FAISS**（零依赖，本地运行），可切换 Milvus：

```env
# FAISS（默认）
USE_MILVUS=false
VECTOR_STORE_PERSIST_DIR=./faiss_index

# Milvus（生产级）
USE_MILVUS=true
MILVUS_HOST=localhost
MILVUS_PORT=19530
```

---

## 7. 核心模块设计

### 7.1 RouterAgent — 意图分类

**输入：** 用户消息文本
**输出：** 意图类别 + 复杂度判断

**8 类意图：**

| 意图 | 触发示例 | 分发目标 | 复杂度 |
|------|---------|---------|--------|
| `shopping` | "推荐洗面奶" | ShoppingAgent | 简单 |
| `knowledge` | "这款手机参数" | KnowledgeQAAgent | L1/L2/L3 |
| `chitchat` | "你好" | ChitChatAgent | 简单 |
| `comparison` | "A和B哪个好" | ShoppingAgent | 简单 |
| `cart` | "加购物车" | ShoppingAgent | 简单 |
| `admin` | "查看销售报表" | AdminCopilotAgent | 中等 |
| `reasoning` | "帮我分析..." | ReasoningAgent | 复杂 |
| `photo` | 拍照上传 | ShoppingAgent | 中等 |

**复杂度路由：**
- 简单 → RouterAgent 直接分发到子 Agent，~2s
- 中等 → 子 Agent + Tool 调用，~4s
- 复杂 → Orchestrator → Planner → Executor 完整编排，~8s

### 7.2 ShoppingAgent — 购物导购

核心 Agent，1700+ 行代码，负责：

**RAG 链路（7 步）：**
1. 意图检测 → 识别对比/购物车/普通推荐
2. 会话记忆读取 → 获取对话历史
3. FAISS 向量检索 → 语义匹配商品知识（k=8, 阈值 0.6）
4. MySQL 四级搜索流水线：
   - Step 1: LLM 提取排除词（"不要优衣库" → ["优衣库"]）
   - Step 2: LLM + 正则提取搜索关键词（双保险）
   - Step 3: 三级优先级匹配（精确 > 泛词展开 > 拆字匹配）
   - Step 4: SQL 参数化搜索 + 品类过滤 + 排除词过滤
5. 结果合并去重 + 加权排序（评分 60% + 销量 40%）
6. LLM 生成推荐话术
7. 构建商品卡片 JSON + 推荐日志记录

**购物车管理：**
- 自然语言指令识别：加购/删除/批量删除/查看/清空
- 会话级购物车缓存（30s TTL）
- 确认卡片交互（删除前确认）

### 7.3 KnowledgeQAAgent — 三级知识问答

| 级别 | 复杂度 | 处理方式 | 示例 |
|------|--------|---------|------|
| L1 | 简单 | 直接从商品字段回答 | "这款手机多少钱" |
| L2 | 中等 | 向量检索 + LLM 生成 | "敏感肌适合什么面膜" |
| L3 | 复杂 | Orchestrator 完整编排 | 多步推理、多文档综合 |

### 7.4 五层编排框架

```
Orchestrator.run()
  ├── GuardrailsPolicy  → 输入校验（长度/安全/格式）
  ├── Planner.plan()    → 生成执行步骤列表
  ├── Executor.execute() → 循环执行每个 step
  │     ├── step_type: tool_call → 调用 ToolRegistry
  │     ├── step_type: llm_call  → 调用 LLM
  │     └── step_type: condition → 条件分支
  ├── EventBus.publish() → 事件监控
  └── RetryPolicy       → 失败重试（max_retries=3）
```

### 7.5 ToolRegistry — 工具注册体系

**已注册工具（7 个）：**

| 工具 | 功能 | 调用场景 |
|------|------|---------|
| `knowledge_search` | 知识库语义检索 | KnowledgeQAAgent |
| `question_rewrite` | 问题改写 + 画像感知 | ShoppingAgent / RetrievalAgent |
| `rerank` | 检索结果重排序 | KnowledgeQAAgent |
| `memory_read` | 会话记忆读取 | 所有 Agent |
| `memory_write` | 会话记忆写入 | RouterAgent（统一入口） |
| `ocr_extract` | 图片文字提取 | 拍照识图 |
| `cart_tool` | 购物车 CRUD | ShoppingAgent |

**调用链路：**
```
Agent → tool_registry.invoke_tool("tool_name", params, run_id)
  → 参数校验 (validate_input)
  → 超时控制 (共享线程池 max_workers=10)
  → 重试机制 (max_retries)
  → 执行跟踪 (ToolExecutionTracker, TTL 3600s, 容量 1000)
```

### 7.6 SSE 流式通信

**事件格式：**

```json
{"type": "routed", "task_type": "shopping"}
{"type": "token", "content": "推荐"}
{"type": "token", "content": "一款"}
{"type": "product_cards", "product_cards": [...], "sources": [...]}
{"type": "confirm_card", "confirm_card": {...}}
{"type": "cart_selection", "cart_list": [...]}
{"type": "end"}
{"type": "error", "content": "错误信息"}
```

**链路：**
```
Python FastAPI (SSE Generator)
  → Java WebFlux (SSE 透传)
  → Android OkHttp SSE (EventSourceListener)
  → ChatViewModel (StateFlow)
  → ChatScreen (Compose 逐字渲染)
```

### 7.7 四级记忆体系

系统采用分层记忆架构，短期记忆、会话记忆、用户记忆、知识记忆分层管理，按需选择读写速度和生命周期最合适的存储层。

```
  Layer 1: 短期记忆（AgentState）
  ├── 内容：当前对话的临时状态
  ├── 存储：进程内存（Python dataclass）
  ├── 生命周期：单次请求
  ├── 读写速度：零网络开销
  └── 用途：步骤列表、中间结果、状态机

  Layer 2: 会话记忆（Redis）
  ├── 内容：最近 N 轮对话历史
  ├── 存储：Redis（TTL 24 小时）
  ├── 生命周期：会话级别
  ├── 读写速度：毫秒级
  ├── 核心机制：对话压缩（超长时 LLM 自动摘要）
  └── 用途：上下文连续性、多轮对话

  Layer 3: 用户记忆（MySQL）
  ├── 内容：用户偏好、历史问答、未回答问题
  ├── 存储：MySQL 持久化
  ├── 生命周期：永久
  └── 用途：个性化推荐、用户画像

  Layer 4: 知识记忆（向量库）
  ├── 内容：知识库文档的向量表示
  ├── 存储：FAISS（本地）/ Milvus（生产）
  ├── 生命周期：随知识库更新
  └── 用途：语义相似度检索
```

**记忆注入方式：**

Agent 执行时，Planner 自动在流程开始时插入 `memory_read` 步骤，在流程结束时插入 `memory_write` 步骤。读取的记忆拼接到 Prompt 的 context 中，LLM 基于上下文生成更准确的回答。

**会话记忆压缩机制：**

当对话轮数超过 10 轮时，自动触发 LLM 摘要压缩：
- 保留最近 5 轮完整对话
- 早期对话压缩为摘要（2-3 句话概括关键信息）
- 摘要缓存到 Redis，下次读取时直接使用

**用户偏好自动提取：**

每次对话结束后，异步调用 LLM 分析用户问答，提取偏好特征（如"偏好简洁回复"、"关注护肤品"），写入 MySQL `user_memory` 表，用于后续个性化推荐。

**关键代码：**
- `agent/state.py` — `AgentState` 短期记忆（run_id / trace_id / steps / tool_calls）
- `tools/memory_read.py` — 会话记忆读取 + 自动压缩
- `tools/memory_write.py` — 会话记忆写入
- `agent/memory_agent.py` — 记忆管理 Agent（加载 + 保存 + 偏好提取）
- `core/mysql_client.py` — `UserMemoryClient` 用户记忆持久化
- `core/vector_store.py` — `VectorStoreManager` 知识向量检索

### 7.8 全链路可观测性设计

系统内置 runId/traceId 全链路追踪，通过 EventBus 事件驱动机制实现完整可观测性。

**事件驱动追踪（agent/events.py）：**

```
  事件类型：
  ├── RunStartedEvent    → Agent 开始执行
  ├── StepStartedEvent   → 步骤开始
  ├── StepCompletedEvent → 步骤完成（含耗时、输出）
  ├── StepFailedEvent    → 步骤失败（含错误信息）
  ├── RunCompletedEvent  → Agent 执行完成
  └── RunFailedEvent     → Agent 执行失败

  每个事件携带：
  ├── run_id: 唯一标识一次 Agent 执行
  ├── trace_id: 跨服务追踪（Java 后端 → Python AI 服务）
  ├── step_id: 步骤标识
  ├── step_type: 步骤类型（intent_recognition / knowledge_search / ...）
  ├── duration_ms: 执行耗时
  └── output/error: 输出或错误信息
```

**MetricsCollector 指标收集：**

- 订阅所有事件，自动统计成功/失败次数、耗时
- 连续失败 3 次触发告警
- 支持注册自定义告警回调

**管理端展示（Agent 执行记录页面）：**

- 执行列表：所有 Agent 运行记录（runId、状态、耗时）
- 步骤详情：每个步骤的输入/输出/耗时
- 工具调用：每次 Tool 调用的参数和结果
- 时间线：从用户提问到最终回答的完整时间线

**关键代码：**
- `agent/events.py` — EventBus 事件总线 + MetricsCollector 指标收集
- `agent/state.py` — `AgentState` / `AgentStep` / `ToolCallRecord` 状态追踪
- `core/mysql_client.py` — `insert_agent_run()` / `insert_agent_step()` 持久化存储

---

## 8. 关键问题解决方案

### 8.1 RAG 检索精度优化

**问题：** 用户说"推荐跑鞋"，拆字逻辑把"跑"和"鞋"单独匹配，导致篮球鞋也混入结果。

**解决方案：** 四级搜索流水线

```
用户问题
  → Step 1: LLM 提取排除词（"不要优衣库" → ["优衣库"]）
  → Step 2: LLM + 正则提取搜索关键词（双保险）
  → Step 3: 三级优先级匹配
       精确匹配: "跑鞋" → 只匹配"跑步鞋"，不展开
       泛词展开: "鞋子" → ["篮球鞋","跑步鞋","徒步鞋","运动鞋"]
       拆字匹配: 中文单字拆分模糊搜索
  → Step 4: SQL 参数化搜索 + 品类过滤 + 排除词过滤
  → Step 5: 后处理 → 去重 + 状态过滤 + 加权排序（评分60% + 销量40%）
```

**关键代码：** `workflows/shopping_agent.py` — `_search_products()`

### 8.2 防幻觉机制

**问题：** LLM 可能编造不存在的商品、价格、评分等数据。

**三层防幻觉：**

| 层级 | 机制 | 说明 |
|------|------|------|
| Prompt 约束 | LLM Prompt 明确指令 | "不要编造价格/评分/销量数据" |
| 输出校验 | validateProductCards() | 用 product_id 查数据库，比对价格/标题/状态，不一致则覆盖 |
| 参数化查询 | SQL 注入修复 | CASE 表达式从 f-string 改为参数化查询 |

**关键代码：**
- `service/impl/ChatServiceImpl.java` — `validateProductCards()`
- `workflows/shopping_agent.py` — `_validate_llm_prices()`

### 8.3 多轮上下文记忆

**问题：** 用户说"推荐跑鞋"后说"要轻量的"，Agent 需要理解"轻量的"指的是上一轮的跑鞋。

**解决方案：**
- 会话记忆读取：每次请求前读取 `conversation_context` 表
- 记忆写入：RouterAgent 统一入口写入（避免重复写入）
- 上下文感知：LLM Prompt 注入对话历史，理解"那个"、"它"等指代

### 8.4 反选排除

**问题：** 用户说"推荐防晒霜，但不要含酒精的"，需要在检索阶段排除含酒精商品。

**解决方案：**
- LLM 提取排除关键词 → `["酒精"]`
- SQL 搜索时增加排除条件：`WHERE tags NOT LIKE '%酒精%'`
- 后处理阶段再次过滤

### 8.5 语音输入

**链路：**
```
Android MediaRecorder 录音 (MPEG_4/AAC, 16kHz)
  → 上传音频文件 (Multipart)
  → Java 透传 → Python /api/voice/recognize
  → 豆包多模态模型 ASR（非独立 ASR 服务）
  → 返回文字 → 填入输入框 → 用户确认后发送
```

### 8.6 拍照识图

**链路：**
```
Android 拍照/选图
  → 上传图片 (Multipart)
  → Java 透传 → Python /api/recognize-image
  → 豆包 Vision API 理解图片语义
  → 返回描述（如"一双白色Nike运动鞋"）
  → 作为搜索词走 ShoppingAgent 检索链路
  → 返回推荐商品 + 商品卡片
```

### 8.7 对话式购物车管理

**问题：** 用户通过自然语言控制购物车，Agent 需要精准识别多种操作意图。

**支持指令：**

| 操作 | 示例 |
|------|------|
| 加购 | "把这个加到购物车" |
| 泛化加购 | "添加购物车"（显示选择卡片） |
| 删除 | "删除第二个商品" |
| 批量删除 | "删除前两个" / "清空购物车" |
| 查看 | "看看我的购物车" |

**实现：** 正则意图识别 + 商品 ID 解析 + 确认卡片交互 + 30s 会话缓存

### 8.8 IDOR 安全漏洞修复

**问题：** 部分接口的 userId 由客户端传入，存在越权风险。

**解决方案：** Java 端统一从 JWT Token 获取用户身份：
```java
SecurityContextHolder.getContext().getAuthentication().getName()
```

---

## 9. API 接口清单

### 9.1 用户端 API

| 模块 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 认证 | `/api/auth/login` | POST | 手机号+密码登录 |
| 认证 | `/api/auth/register` | POST | 注册 |
| 认证 | `/api/auth/send-code` | POST | 发送验证码 |
| 认证 | `/api/auth/change-password` | POST | 修改密码 |
| 对话 | `/api/chat/stream/messages` | POST | SSE 流式对话 |
| 对话 | `/api/chat/messages` | POST | 非流式对话 |
| 对话 | `/api/chat/conversations` | GET | 会话列表 |
| 对话 | `/api/chat/conversations/{id}/messages` | GET | 历史消息 |
| 对话 | `/api/chat/voice/recognize` | POST | 语音识别 |
| 对话 | `/api/chat/recognize-image` | POST | 图片识别 |
| 商品 | `/api/product/list` | GET | 商品列表 |
| 商品 | `/api/product/{id}` | GET | 商品详情 |
| 商品 | `/api/product/categories` | GET | 品类列表 |
| 购物车 | `/api/cart/list` | GET | 购物车列表 |
| 购物车 | `/api/cart/add` | POST | 加购 |
| 购物车 | `/api/cart/{id}` | DELETE | 删除 |
| 订单 | `/api/order/create` | POST | 创建订单 |
| 订单 | `/api/order/list` | GET | 订单列表 |
| 订单 | `/api/order/{id}` | GET | 订单详情 |
| 推荐 | `/api/recommend` | GET | 个性化推荐 |
| 收藏 | `/api/favorite/*` | GET/POST/DELETE | 收藏 CRUD |
| 地址 | `/api/address/*` | GET/POST/PUT/DELETE | 地址 CRUD |

### 9.2 管理端 API

| 模块 | 接口 | 说明 |
|------|------|------|
| 认证 | `POST /api/admin/login` | 管理员登录 |
| 仪表盘 | `GET /api/admin/dashboard/stats` | 数据统计 |
| 商品 | `GET/PUT /api/admin/product/*` | 商品管理 |
| 订单 | `GET /api/admin/order/*` | 订单管理 |
| 对话 | `GET /api/admin/conversation/*` | 对话管理 |
| 知识库 | `POST/GET/DELETE /api/admin/knowledge/*` | 知识库管理 |
| Agent | `GET /api/admin/agent/*` | Agent 执行记录 |
| 用户 | `GET /api/admin/users` | 用户列表 |

### 9.3 Python AI 服务 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/chat/stream` | POST (SSE) | 流式对话 |
| `/api/chat` | POST | 非流式对话 |
| `/api/voice/recognize` | POST | 语音识别 |
| `/api/recognize-image` | POST | 图片识别 |
| `/api/vector-store/*` | GET/POST/DELETE | 向量库管理 |
| `/api/knowledge/upload` | POST | 知识库上传 |
| `/api/inspection/*` | GET | 知识巡检 |

---

## 10. 开发过程中遇到的问题与解决方案

### 10.1 LLM 输出截断问题

**问题：** 使用 qwen-plus 模型时，即使在 Prompt 中指定了回复长度限制，模型仍然经常忽略指令，生成超长回复导致 token 浪费和响应延迟。

**解决方案：** 在代码层面实现双重截断保障：
- **Prompt 级：** 在 System Prompt 中明确指令"控制在 X 字以内"
- **Token 级：** 配置 `max_tokens` 参数硬性限制输出长度
- **代码级：** 生成完成后对输出文本做字符数截断，超长部分直接裁剪并补充省略号

三层保障确保无论模型是否遵守指令，最终输出都在可控范围内。

### 10.2 工具注册中心线程池问题

**问题：** `tools/registry.py` 中的 `invoke_tool()` 方法每次调用都创建一个新的 `ThreadPoolExecutor(max_workers=1)`，高频调用时线程创建/销毁开销大，且无法复用线程。

**解决方案：** 改为共享线程池模式：
- 在 ToolRegistry 初始化时创建一个全局共享的 `ThreadPoolExecutor(max_workers=10)`
- 所有 `invoke_tool()` 调用复用同一个线程池
- 线程池生命周期与应用一致，避免反复创建销毁

### 10.3 工具执行跟踪内存泄漏

**问题：** `tools/execution.py` 中的 `ToolExecutionTracker` 只记录不清理，执行记录无限增长，长时间运行后内存持续膨胀。

**解决方案：** 引入容量上限 + TTL 自动淘汰：
- 设置最大容量 1000 条记录
- 每条记录设置 TTL 3600 秒（1 小时）
- 超过容量或 TTL 过期的记录自动淘汰
- 采用 LRU 策略优先淘汰最久未访问的记录

### 10.4 记忆写入重复

**问题：** `base_agent.py` 和 `memory_agent.py` 两处都在写入会话记忆，导致同一条对话被重复记录，记忆表数据冗余且可能干扰上下文检索。

**解决方案：** 统一记忆写入入口：
- 记忆写入统一收敛到 RouterAgent（`router_agent.py`），作为唯一的记忆写入点
- 子 Agent（ShoppingAgent、ChitChatAgent 等）不再自行写入记忆
- 通过 `memory_write` 工具统一管理，避免重复写入

### 10.5 流式模式未读取会话记忆

**问题：** `chitchat_agent.py` 的 `chat_stream()` 方法没有读取会话记忆，导致流式模式下 Agent 无法记住之前的对话内容，多轮对话体验断裂。

**解决方案：** 在流式方法开头补充记忆读取逻辑：
- 调用 `memory_read` 工具获取当前会话的历史记忆
- 将记忆内容注入 LLM Prompt 的上下文部分
- 确保流式和非流式两种模式的记忆一致性

### 10.6 Agent 绕过 ToolRegistry 直接实例化工具

**问题：** 部分 Agent（如 `retrieval_agent.py`）直接实例化工具类，绕过了 ToolRegistry 的统一管理，导致工具调用没有执行跟踪、超时控制和重试机制。

**解决方案：** 统一通过 ToolRegistry 调用：
- 所有工具调用必须通过 `tool_registry.invoke_tool("tool_name", params, run_id)`
- 禁止直接实例化工具类
- 在代码审查中加入此规范检查

### 10.7 IDOR 安全漏洞

**问题：** `ChatController.sendMessage` 等接口的 userId 由客户端请求参数传入，攻击者可以篡改 userId 访问其他用户的对话数据，存在越权（IDOR）风险。

**解决方案：** 服务端统一身份校验：
- Java 端通过 `SecurityContextHolder.getContext().getAuthentication().getName()` 从 JWT Token 中获取用户身份
- 不再信任客户端传入的 userId
- 所有涉及用户数据的接口统一校验

### 10.8 SSE 流式通信的 Android 端适配

**问题：** Android 端使用 OkHttp 接收 SSE 流式响应时，遇到多个问题：
- 连接超时设置不合理导致长回复被截断
- JSON 解析异常时整个流中断
- 错误事件和正常结束事件的区分

**解决方案：**
- 将 `readTimeout` 从秒级改为 5 分钟，适配长回复场景
- 每个 SSE 事件独立 try-catch，单个事件解析失败不影响整体流
- 通过事件类型字段（`end` / `error`）明确区分正常结束和异常

### 10.9 键盘弹起遮挡输入框

**问题：** Android 对话页点击输入框弹起键盘时，输入框被键盘遮挡，或输入框与键盘之间出现空白间隙。

**解决方案：**
- `AndroidManifest.xml` 中配置 `android:windowSoftInputMode="adjustResize"`
- Compose 层使用 `imePadding()` 处理 IME 避让
- 移除外层 Column 的 `windowInsetsPadding(WindowInsets.navigationBars)` 避免双重 padding

---

## 11. 项目创新点

### 11.1 从"看广告"到"对话购物"——交互范式转变

**传统模式局限：** 静态图文/短视频，单向"看与被看"，用户只能被动浏览。

**本项目方案：** 用户通过自然语言对话表达需求，AI 理解意图后主动推荐。

```
传统模式：用户浏览 → 看到商品 → 自行判断 → 决定购买
本项目：  用户说"推荐一款适合油皮的洗面奶" → AI 理解肤质需求 → 检索匹配 → 生成个性化推荐话术 + 商品卡片
```

这不是简单的"搜索框"，而是真正的**对话式导购**——AI 能理解模糊需求、追问细节、记住上下文，像一个懂商品的真人导购。

### 11.2 模糊需求理解——解决"用户说不清"的痛点

**传统模式局限：** 用户产生模糊兴趣时无法深度互动。比如用户想买"一个不贵又好用的耳机"，传统电商只能靠关键词匹配，结果要么太多要么不准。

**本项目方案：** LLM 理解模糊语义 + RAG 精准检索：

```
用户: "推荐一个不贵的耳机"
  → LLM 理解："不贵"= 价格敏感，"耳机"= 品类
  → RAG 检索：按价格升序 + 评分加权排序
  → 返回：性价比最高的几款 + 推荐理由
```

项目实现了**四级搜索流水线**（排除词提取 → 关键词提取 → 三级优先级匹配 → 参数化查询），能处理"跑鞋"（不拆字）、"不要优衣库"（反选排除）、"敏感肌用的"（肤质过滤）等复杂语义。

### 11.3 多模态交互——降低表达门槛

**传统模式局限：** 用户只能通过文字搜索或分类浏览找到商品，表达方式单一。

**本项目方案：** 支持文字、语音、拍照三种交互模态：

| 模态 | 场景 | 实现 |
|------|------|------|
| 文字 | 常规对话 | SSE 流式输出，逐字渲染 |
| 语音 | 不方便打字时 | MediaRecorder 录音 → 豆包多模态 ASR → 文字填入输入框 |
| 拍照 | 看到同款想找 | 拍照/选图 → 豆包 Vision API 理解图片 → 提取商品特征 → 搜索推荐 |

拍照识图不是简单的 OCR 文字提取，而是**语义理解**——拍一张街拍照片，AI 能识别出"白色Nike运动鞋"并搜索同款。

### 11.4 反选排除——精准过滤不需要的商品

**传统模式局限：** 传统电商筛选器是固定维度（价格/品牌/评分），无法表达"不要什么"。

**本项目方案：** 支持自然语言否定语义：

```
用户: "推荐防晒霜，但不要含酒精的"
  → LLM 提取排除词：["酒精"]
  → 检索时过滤：WHERE tags NOT LIKE '%酒精%'
  → 返回：不含酒精的防晒霜
```

这一能力在传统电商中很难实现——用户需要手动在筛选器中逐个排除，而在本项目中一句话就能完成。

### 11.5 渐进式需求收敛——多轮对话逐步精准

**传统模式局限：** 每次搜索都是独立的，用户需要反复调整关键词，之前的搜索意图不会被记住。

**本项目方案：** 多轮上下文记忆 + 渐进式过滤：

```
用户: "推荐跑鞋"
AI:   [推荐5款跑鞋]
用户: "要轻量的"
AI:   [从上一轮结果中筛选轻量款]  ← 不是重新搜索，而是在已有结果中过滤
用户: "不要超过五百"
AI:   [进一步按价格过滤]
```

Agent 通过会话记忆记住之前的推荐结果和用户偏好，每轮对话都在前一轮基础上进一步收敛，而不是从零开始。

### 11.6 对话式购物车管理——自然语言替代手动操作

**传统模式局限：** 购物车管理需要手动点击"加入购物车"按钮、勾选删除等机械操作。

**本项目方案：** 通过自然语言指令管理购物车：

```
用户: "把这个加到购物车"        → Agent 识别商品 → 执行加购
用户: "删除第二个商品"          → Agent 解析"第二个" → 定位商品 → 确认卡片
用户: "清空购物车"              → Agent 批量操作 → 确认卡片
```

Agent 需要维护对话上下文中的商品列表状态，理解"这个"、"第二个"等指代词，并通过确认卡片让用户在执行前确认，避免误操作。

### 11.7 三层防幻觉机制——确保推荐可信

**传统模式局限：** 传统电商的商品信息来自数据库，不存在"编造"问题。但 AI 推荐系统的核心风险就是 LLM 幻觉——编造不存在的商品、虚高的评分、错误的价格。

**本项目方案：** 三层防幻觉保障：

| 层级 | 机制 | 作用 |
|------|------|------|
| Prompt 约束 | 明确指令"不要编造" | 从源头减少幻觉概率 |
| 输出校验 | `validateProductCards()` | 用 product_id 查数据库比对，不一致则用真实数据覆盖 |
| 参数化查询 | SQL 注入修复 | 防止恶意输入篡改查询逻辑 |

最终效果：推荐的商品一定存在于数据库中，价格、评分、库存等信息与数据库一致。

### 11.8 五层可编排 Agent 架构——兼顾效率与能力

**传统模式局限：** 单一 LLM 调用无法处理复杂的多步推理任务；但所有请求都走完整编排又会增加简单请求的延迟。

**本项目方案：** 分层路由，按复杂度选择执行路径：

| 复杂度 | 路径 | 示例 | 延迟 |
|--------|------|------|------|
| 简单 | RouterAgent → 子 Agent 直接返回 | "推荐跑鞋" / "你好" | ~2s |
| 中等 | RouterAgent → 子 Agent → Tool 调用 | "这款手机参数是什么" | ~4s |
| 复杂 | RouterAgent → Orchestrator → Planner → Executor | 多步知识问答 | ~8s |

简单请求走最短路径快速响应，复杂请求走完整编排保证错误重试、状态追踪、事件监控。这比"所有请求都走编排"或"所有请求都直接调用 LLM"都更合理。

---

## 附录

### A. 启动顺序

```
1. docker-compose up -d          # MySQL + Redis
2. cd backend/python-service && python main.py   # Python :8000
3. cd backend && mvn spring-boot:run             # Java :8888
4. cd backend/frontend && npm run dev            # 前端 :5173 (可选)
5. Android Studio 编译运行                       # App
```

### B. 默认账号

| 角色 | 账号 | 密码 |
|------|------|------|
| 管理员 | admin | admin123 |

### C. 数据集

4 品类 × 25 商品 = 100 条脱敏电商数据：
- 美妆护肤（25 件）
- 数码电子（25 件）
- 服饰运动（25 件）
- 食品生活（25 件）
